#include<stdio.h>
int main()
{
int i,j,rows;
printf("enter the number of rows:\n");
scanf("%d",&rows);
printf("\n");
for(i=1;i<=rows;i++)
{
  for(j=1;j<=2*i;j++)
  {
    if(j%2==0)
    
      printf("1");
    else
      printf("0");
  }
  printf("\n");
  
}
return 0;
}
