#include<stdio.h>
int main()
{
	int n,i;
	printf("enter the no.:\n");
	scanf("%d",&n);
	i=2;
	while(i<=n-1)
	{
		if(n%i==0)
		{
			printf("%d is not a prime number",n);
			break;
		}
		i++;
	}
	if(i==n)
	printf("%d is a prime number",n);
}
